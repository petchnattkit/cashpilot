# Task: Implement Human Scenario Tests

## User Story
**As** a QA Engineer,
**I want** to simulate a real user journey (Create SKU -> Settings -> Transaction -> Dashboard),
**So I can** verify the features work together in harmony.

## Atomic Scope
- Create `src/tests/integration/transaction.test.tsx` (Domain Driven Test).
- Scenario:
    1. Mock SKU and Category data.
    2. Render App.
    3. Verify Transaction list displays correct linked data.
    4. Verify Dashboard widgets calculation (if possible in integration test) or Mock Dashboard hooks.

## Acceptance Criteria
1. Run `npm test src/tests/integration/transaction.test.tsx`.
2. Test PASSES.
3. Verify test logs show correct component rendering (no "Loading..." infinite states).

## Impact Area
- Testing Suite
